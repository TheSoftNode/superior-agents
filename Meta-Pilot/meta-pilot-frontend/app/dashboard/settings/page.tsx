import SettingsPage from '@/components/Dashbaord/Settings/SettingsPage'
import React from 'react'

type Props = {}

const page = (props: Props) => {
    return (
        <SettingsPage />
    )
}

export default page